<!-- Vehicle item ejs wrapped in a section -->
<section class="vehicle_section">
    <!-- Content Heading -->
    <% if (title) { %>
        <h1><%= title %></h1>
        <% } else {
          res.redirect('/')
        } %>
    <!-- Message Block -->
    <%# messages() %>
    <!-- Vehicle Content -->
    <%- card %>
    </section>